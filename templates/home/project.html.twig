<section id="project" class="py-5" style="background-color: #f5f5e9;">
	<div class="container">
		<div class="row align-items-center">
			<div class="col-md-6 text-center mb-4 mb-md-0">
				<img src="{{ asset('img/Emma.png') }}" alt="{{ 'featurette.photo_alt'|trans }}" class="img-fluid rounded shadow" style="max-height: 600px; object-fit: cover;" loading="lazy">
			</div>

			<div class="col-md-6">
				<h2 class="fw-bold mb-3">{{ 'featurette.heading1'|trans }}</h2>
				<h3 class="text-muted mb-4">{{ 'featurette.heading2'|trans }}</h3>

				<p class="lead mb-4">
					{{ 'featurette.description1'|trans|raw }}

				</p>

				<a href="{{ path('services_index') }}" class="btn-custom btn-outline-dark px-4 py-2">
					{{ 'En savoir plus'|trans }}
				</a>
			</div>
		</div>
	</div>
</section>
