{% extends 'base.html.twig' %}

{% block title %}
	{{ 'sitemap.title'|trans }}
{% endblock %}

{% block body %}
	<section class="legal-page container py-5">
		<h1 class="text-center mb-4">{{ 'sitemap.heading'|trans }}</h1>

		<div class="card mb-4 p-4 shadow-sm">
			<h2>{{ 'sitemap.main_navigation'|trans }}</h2>
			<ul>
				<li><ahref="{{path('homepage')}}">{{ 'menu.home'|trans }}</a></li>
				<li><ahref="{{path('services_index')}}">{{ 'menu.services'|trans }}</a></li>
				<li><ahref="{{path('blog_display')}}">{{ 'menu.blog'|trans }}</a></li>
				<li><ahref="{{path('contact_index')}}">{{ 'menu.contact'|trans }}</a></li>
			</ul>
		</div>

		<div class="card mb-4 p-4 shadow-sm">
			<h2>{{ 'sitemap.legal_pages'|trans }}</h2>
			<ul>
				<li><ahref="{{path('legal_all')}}">{{ 'sitemap.all_legal'|trans }}</a></li>
				<li><ahref="{{path('mentions_legales')}}">{{ 'sitemap.legal_mentions'|trans }}</a></li>
				<li><ahref="{{path('politique_confidentialite')}}">{{ 'sitemap.privacy_policy'|trans }}</a></li>
				<li><ahref="{{path('cgv')}}">{{ 'sitemap.terms_of_sale'|trans }}</a></li>
				<li><ahref="{{path('cgu')}}">{{ 'sitemap.terms_of_use'|trans }}</a></li>
				<li><ahref="{{path('politique_remboursement')}}">{{ 'sitemap.refund_policy'|trans }}</a></li>
				<li><ahref="{{path('politique_cookies')}}">{{ 'sitemap.cookies_policy'|trans }}</a></li>
			</ul>
		</div>
	</section>
{% endblock %}
